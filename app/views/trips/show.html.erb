<h1><%= @trip.name %></h1>
<p><a href="/instagram/connect">Fetch Photos From Instagram</a></p>
<p><a href="/facebook/connect">Fetch Photos From Facebook</a></p>


<div class="map-wrapper">
  <div id="map-canvas"></div>
</div>

<div class="photos-wrapper">
  <% if @photos %>
    <% process_photos(@photos).each_with_index do |photo|%>
      <div class="photo-container" data-lat="<%= photo.lat %>" data-lng="<%= photo.long %>">
          <img src="<%= photo.url %>" ><br>
      </div>
    <% end %>
  <% end %>
</div>