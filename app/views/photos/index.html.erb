<div class="container">
	<h1>Photos</h1>

	<% if @photos %>
		<p>Photo List:</p>
		<p>Trip ID in session: <%= session[:current_trip] %></p>
		<ul>
			<% @photos.each do |photo| %>
				<li><%= link_to photo.url, photo_path(photo),:target => "_blank" %> - Trip: <%= photo.trip_id %></li>
			<% end %>
		</ul>
	<% end %>
</div>

